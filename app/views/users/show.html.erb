<h1 class="text-center mt-4"><%= @user.username %>'s profile posts</h1>
<div class="flickr_pagination">
  <%= will_paginate @posts, :container => false %>
</div>
<%= render 'posts/post' %>
<div class="flickr_pagination">
  <%= will_paginate @posts, :container => false %>
</div>